export default (function active () {
  function activeFilter () {
    /*
    return this
      .concat()
      .sort(function (a, b) {
        return a.active - b.active
      })
    */
    return this
  }

  return activeFilter
}())
